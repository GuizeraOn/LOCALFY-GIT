---
phase: "07"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - "src/app/api/sync/hotmart/route.ts"
  - "src/app/api/webhook/hotmart/route.ts"
autonomous: true
requirements:
  - UI-05
requirements_addressed:
  - UI-05

must_haves:
  truths:
    - "Importar histórico da Hotmart grava product_id e product_name em cada venda"
    - "Re-executar a importação sobre o mesmo período faz backfill do produto nas vendas já existentes, sem duplicar linhas"
    - "O webhook de vendas novas também grava o produto quando a Hotmart o envia"
  artifacts:
    - path: "src/app/api/sync/hotmart/route.ts"
      provides: "Persistência de product_id/product_name no upsert de sales"
      contains: "product_id"
    - path: "src/app/api/webhook/hotmart/route.ts"
      provides: "Captura de produto no webhook"
      contains: "product_id"
  key_links:
    - from: "src/app/api/sync/hotmart/route.ts"
      to: "src/lib/hotmart.ts"
      via: "m.sale.product_id / m.sale.product_name vindos de mapHotmartItem"
      pattern: "m\\.sale\\.product_"
    - from: "src/app/api/sync/hotmart/route.ts"
      to: "tabela sales"
      via: "upsert com onConflict transaction_id"
      pattern: "onConflict: 'transaction_id'"
---

<objective>
Persistir o produto de cada venda no banco: o endpoint de importação de histórico (`POST /api/sync/hotmart`) e o webhook (`POST /api/webhook/hotmart`) passam a gravar `product_id` e `product_name` na tabela `sales`.

Purpose: Sem isso, as colunas criadas em 07-01 ficam vazias e o dropdown de produto não teria opções. Como o upsert usa `onConflict: 'transaction_id'`, re-executar "Importar Histórico" sobre o período completo funciona como mecanismo de backfill das vendas já importadas na Phase 6.
Output: Rotas de sync e webhook gravando produto; contagem de vendas com produto retornada pelo endpoint de sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-importa-o-de-hist-rico-hotmart/06-02-SUMMARY.md

@src/app/api/sync/hotmart/route.ts
@src/app/api/webhook/hotmart/route.ts
@src/lib/hotmart.ts
</context>

<interfaces>
<!-- Após o plano 07-01, mapHotmartItem devolve produto: -->

```typescript
export interface HotmartSaleRow {
  transaction_id: string;
  status: string;
  price: number;
  created_at: string | null;
  product_id: string | null;
  product_name: string | null;
}
```

<!-- Tipo local existente em src/app/api/sync/hotmart/route.ts (precisa ser estendido) -->

```typescript
interface SaleRow {
  transaction_id: string;
  status: string;
  price: number;
  updated_at: string;
  created_at?: string;   // omitido quando a Hotmart não deu data
}
```

<!-- Resposta atual de POST /api/sync/hotmart (consumida por DashboardHeader.handleImportHistory) -->

```typescript
{ success: true, count: number, total: number, skipped: number,
  nextPageToken: string | null, totalResults: number | null }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Gravar produto no import de histórico</name>
  <files>src/app/api/sync/hotmart/route.ts</files>
  <read_first>
    - src/app/api/sync/hotmart/route.ts (interface local SaleRow, passo 10 "Persistir", passo 11 do retorno)
    - src/lib/hotmart.ts (campos product_id/product_name adicionados em 07-01)
  </read_first>
  <action>
    Em `src/app/api/sync/hotmart/route.ts`:

    1. Estender a interface local `SaleRow` com `product_id: string | null;` e `product_name: string | null;` (obrigatórios, diferente de `created_at`, porque sempre temos o valor — ainda que null — e gravar null explicitamente é o que faz o backfill limpar/atualizar a coluna).
    2. No `map` que constrói `saleRows` (passo 10), incluir `product_id: m.sale.product_id` e `product_name: m.sale.product_name` no objeto retornado. NÃO envolver em condicional: null deve ir para o banco.
    3. No retorno JSON (passo 11), acrescentar `withProduct: saleRows.filter(r => r.product_id !== null).length`. Manter TODOS os campos atuais (`success`, `count`, `total`, `skipped`, `nextPageToken`, `totalResults`) — `DashboardHeader.handleImportHistory` depende deles.
    4. Não alterar autenticação, validação de datas, paginação, tratamento de erro nem a ordem de gravação (sales antes de sale_utms, por causa da FK).
    5. Manter `export const dynamic = 'force-dynamic'` no topo.

    Regra de backfill (documentar como comentário de 2 linhas acima do upsert de `sales`): como o upsert usa `onConflict: 'transaction_id'`, re-executar a importação sobre um período já importado atualiza as linhas existentes e preenche `product_id`/`product_name` sem criar duplicatas.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "product_id" src/app/api/sync/hotmart/route.ts | awk '{ if ($1 >= 3) print "PASS"; else { print "FAIL"; exit 1 } }'</automated>
    <automated>grep -c "nextPageToken\|totalResults\|skipped" src/app/api/sync/hotmart/route.ts | awk '{ if ($1 >= 3) print "PASS"; else { print "FAIL"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `SaleRow` inclui `product_id: string \| null` e `product_name: string \| null`.
    - Todo objeto de `saleRows` carrega os dois campos, inclusive quando são null.
    - A resposta JSON mantém `success`, `count`, `total`, `skipped`, `nextPageToken`, `totalResults` e ganha `withProduct`.
    - `npx tsc --noEmit` sem erros.
    - Nenhuma mudança na lógica de auth (Bearer via `HOTMART_ACCESS_TOKEN`), de validação de data (`DATE_RE`) ou de paginação.
  </acceptance_criteria>
  <done>Importação de histórico grava produto e reporta quantas vendas vieram com produto.</done>
</task>

<task type="auto">
  <name>Task 2: Capturar produto no webhook da Hotmart</name>
  <files>src/app/api/webhook/hotmart/route.ts</files>
  <read_first>
    - src/app/api/webhook/hotmart/route.ts (extração de campos do payload e upsert de sales)
    - src/lib/hotmart.ts (helpers de normalização usados no histórico, para manter coerência de formato)
  </read_first>
  <action>
    Em `src/app/api/webhook/hotmart/route.ts`, extrair o produto do payload do webhook com a mesma ordem de fallback usada no histórico, para que webhook e importação gravem a MESMA chave `product_id` (senão o dropdown mostraria o mesmo produto duas vezes):

    1. Após a extração de `price`, adicionar:
       `const rawProductId = payload.product?.id ?? payload.data?.product?.id ?? payload.purchase?.product?.id ?? payload.product?.ucode ?? payload.data?.product?.ucode ?? null;`
       `const rawProductName = payload.product?.name ?? payload.data?.product?.name ?? payload.purchase?.product?.name ?? null;`
    2. Normalizar com um helper local `const cleanStr = (v: unknown): string | null => { if (typeof v === 'string') { const t = v.trim(); return t.length > 0 ? t : null; } if (typeof v === 'number' && Number.isFinite(v)) return String(v); return null; };`
       `const product_id = cleanStr(rawProductId); const product_name = cleanStr(rawProductName);`
       A conversão de número para string é obrigatória: a coluna é TEXT e `mapHotmartItem` faz a mesma conversão.
    3. Incluir `product_id` e `product_name` no objeto do upsert em `sales`.
    4. Não alterar a verificação de `x-hotmart-hottok` contra `HOTMART_WEBHOOK_TOKEN`, o upsert de `sale_utms`, nem o comportamento de responder 200 quando não há `transaction`.
    5. Manter `export const dynamic = 'force-dynamic'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "product_id\|product_name" src/app/api/webhook/hotmart/route.ts | awk '{ if ($1 >= 3) print "PASS"; else { print "FAIL"; exit 1 } }'</automated>
    <automated>grep -c "x-hotmart-hottok" src/app/api/webhook/hotmart/route.ts | awk '{ if ($1 >= 1) print "PASS"; else { print "FAIL"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - O upsert de `sales` no webhook inclui `product_id` e `product_name`.
    - IDs numéricos são gravados como string (mesma normalização de `mapHotmartItem`).
    - Payload sem produto grava null nas duas colunas e continua respondendo 200.
    - A verificação do header `x-hotmart-hottok` permanece intacta.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>Vendas novas via webhook nascem com produto preenchido quando a Hotmart o envia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hotmart (webhook público) → `POST /api/webhook/hotmart` | Endpoint exposto na internet; payload totalmente não confiável |
| Hotmart REST API → `POST /api/sync/hotmart` | Resposta de terceiro processada e persistida |
| Rotas Next.js → Supabase (service role key) | Escrita privilegiada, ignora RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Spoofing | `POST /api/webhook/hotmart` | mitigate | Verificação existente do header `x-hotmart-hottok` contra `HOTMART_WEBHOOK_TOKEN` preservada sem alteração |
| T-07-10 | Tampering | `product_id`/`product_name` do payload | mitigate | Normalização por `cleanStr`/`toCleanString`: apenas string ou number finito viram valor; objetos, arrays e booleanos viram null |
| T-07-11 | Tampering | upsert em `sales` | mitigate | Escrita via supabase-js `.upsert()` (query parametrizada) — nenhuma concatenação de SQL; `onConflict: 'transaction_id'` mantém idempotência |
| T-07-12 | Information Disclosure | resposta de `POST /api/sync/hotmart` | mitigate | Resposta continua expondo só contadores agregados (`count`, `total`, `skipped`, `withProduct`); corpo de erro da Hotmart segue apenas em `console.error`, nunca no JSON de resposta |
| T-07-13 | Denial of Service | import de histórico | accept | Paginação já limitada a 50 itens por chamada e a `MAX_IMPORT_PAGES = 200` no cliente; endpoint é de uso interno acionado por botão |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado; install descoberto durante a execução exige checkpoint humano bloqueante |
</threat_model>

<verification>
1. `npx tsc --noEmit` → exit 0
2. `npx tsc src/lib/hotmart.ts --outDir .tmp-verify --module es2022 --target es2022 --moduleResolution bundler && node scripts/check-hotmart-mapping.mjs` → exit 0 (nenhuma regressão no mapeador)
3. `npm run build` → exit 0
4. `grep -c product_id src/app/api/sync/hotmart/route.ts` → >= 3
</verification>

<success_criteria>
- `POST /api/sync/hotmart` grava `product_id`/`product_name` e devolve `withProduct`.
- Reexecutar a importação sobre um período já importado atualiza as vendas existentes (backfill) sem duplicar.
- `POST /api/webhook/hotmart` grava produto para vendas novas.
</success_criteria>

<output>
Create `.planning/phases/07-filtro-por-produto/07-03-SUMMARY.md` when done
</output>
