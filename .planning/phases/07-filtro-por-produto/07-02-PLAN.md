---
phase: "07"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/metrics.ts"
  - "scripts/check-metrics-product-filter.mjs"
autonomous: true
requirements:
  - UI-05
requirements_addressed:
  - UI-05

must_haves:
  truths:
    - "calculateMetrics aceita um productId opcional e devolve métricas apenas das vendas daquele produto"
    - "Com filtro de produto ativo, o gasto em ads considerado é somente o das campanhas que geraram vendas daquele produto"
    - "Sem filtro (ou com 'all'), os números são idênticos aos atuais"
  artifacts:
    - path: "src/lib/metrics.ts"
      provides: "calculateMetrics com quarto parâmetro options.productId"
      contains: "productId"
    - path: "scripts/check-metrics-product-filter.mjs"
      provides: "Harness offline de assertions do filtro por produto"
      min_lines: 60
  key_links:
    - from: "scripts/check-metrics-product-filter.mjs"
      to: "src/lib/metrics.ts"
      via: "import dinâmico do módulo compilado em .tmp-verify"
      pattern: "calculateMetrics"
---

<objective>
Ensinar o motor de métricas (`src/lib/metrics.ts`) a calcular tudo restrito a um único produto, mantendo 100% de compatibilidade com o comportamento atual quando nenhum produto é selecionado.

Purpose: Os cards (Faturamento, Lucro, ROAS, CPA, Margem) e a tabela de campanhas são produzidos pela mesma função pura `calculateMetrics`. Filtrar só as vendas no SQL não basta: o gasto em ads continuaria global e destruiria o ROAS. Esta é a única peça com lógica de negócio real da fase, e é pura — logo, verificável offline.
Output: `calculateMetrics(sales, adSpends, expenses, options)` + harness `scripts/check-metrics-product-filter.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/metrics.ts
@scripts/check-hotmart-mapping.mjs
</context>

<interfaces>
<!-- Contrato atual (src/lib/metrics.ts). Sem imports: o módulo compila isolado com tsc. -->

```typescript
export const AD_TAX_PERCENTAGE: number; // NEXT_PUBLIC_AD_TAX_PERCENTAGE, default 0.13

export function calculateMetrics(sales: any[], adSpends: any[], expenses: any[]): {
  global: {
    salesCount: number; grossRevenue: number; netRevenue: number; adSpend: number;
    expenses: number; profit: number; roas: number; roi: number; cpa: number; margin: number;
  };
  campaigns: Array<{
    campaign_id: string; campaign_name: string; spend: number; impressions: number;
    clicks: number; salesCount: number; grossRevenue: number; netRevenue: number;
    profit: number; roas: number; roi: number; cpa: number; margin: number;
  }>;
}
```

<!-- Formato das linhas de venda vindas de supabase `.from('sales').select('*, sale_utms(*)')` -->

```typescript
// sale.product_id e sale.product_name passam a existir após a migração 0003 (plano 07-01)
// sale.sale_utms pode vir como objeto OU como array (relação 1-1 no Postgrest)
{ transaction_id, status, price, created_at, product_id, product_name, sale_utms }
```

<!-- Contrato ALVO (consumido pelo plano 07-04) -->

```typescript
export interface MetricsOptions { productId?: string | null }

export function calculateMetrics(
  sales: any[], adSpends: any[], expenses: any[], options?: MetricsOptions
): { global: { /* ...campos atuais... */ productFilter: string | null }, campaigns: [...] }
```

<!-- Padrão do harness offline (scripts/check-hotmart-mapping.mjs, Phase 6):
     compila o módulo com tsc para .tmp-verify, garante .tmp-verify/package.json
     com {"type":"module"}, importa via pathToFileURL e usa node:assert/strict. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Filtro por produto em calculateMetrics</name>
  <files>src/lib/metrics.ts</files>
  <read_first>
    - src/lib/metrics.ts (função inteira: cálculo global, agrupamento por campanha)
    - src/app/api/metrics/route.ts (para conferir a forma exata dos dados passados hoje)
  </read_first>
  <action>
    Reescrever `src/lib/metrics.ts` mantendo o cálculo existente intacto e adicionando o recorte por produto.

    1. Exportar `export interface MetricsOptions { productId?: string | null }`.
    2. Nova assinatura: `export function calculateMetrics(sales: any[], adSpends: any[], expenses: any[], options: MetricsOptions = {})`. O quarto parâmetro é opcional — chamadas existentes de 3 argumentos continuam válidas.
    3. Normalizar o filtro no topo da função:
       `const rawProduct = typeof options.productId === 'string' ? options.productId.trim() : '';`
       `const productFilter = (rawProduct === '' || rawProduct === 'all') ? null : rawProduct;`
    4. Exportar um helper puro `export function getSaleCampaign(sale: any): string | null` que resolve `sale.sale_utms` tanto quando vem como objeto quanto como array: se for array, usa o primeiro elemento; devolve `utm_campaign` como string não vazia ou `null`. Substituir o acesso atual `sale.sale_utms?.utm_campaign` por essa função no agrupamento de vendas por campanha (o Postgrest pode devolver a relação 1-1 como array — este helper corrige o caso).
    5. Aplicar o filtro ANTES de qualquer cálculo:
       - `const scopedSales = productFilter === null ? sales : sales.filter(s => String(s?.product_id ?? '') === productFilter);`
       - Todo o restante da função passa a usar `scopedSales` no lugar de `sales`.
    6. Restringir o gasto em ads quando `productFilter !== null`:
       - Construir `const campaignIds = new Set<string>()` a partir de `getSaleCampaign(sale)` de cada venda aprovada em `scopedSales` (ignorando `null`).
       - `const scopedAdSpends = productFilter === null ? adSpends : adSpends.filter(ad => campaignIds.has(String(ad.campaign_id)));`
       - Usar `scopedAdSpends` tanto no `rawAdSpend` global quanto no agrupamento `campaignStats`. Assim a tabela de campanhas passa a refletir o filtro de produto (critério de sucesso 3 da fase).
    7. Despesas manuais (`expenses`) são custos globais e NÃO são atribuíveis a um produto específico: quando `productFilter !== null`, usar `const scopedExpenses: any[] = []` (total 0). Documentar essa regra com um comentário de uma linha acima da variável.
    8. Adicionar `productFilter` ao objeto `global` retornado (valor `productFilter`, ou seja `string | null`). Nenhum outro campo do retorno muda de nome ou de fórmula.
    9. Não introduzir imports novos — o módulo precisa continuar compilando isolado com `tsc` (requisito do harness offline).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "scopedSales\|scopedAdSpends\|productFilter" src/lib/metrics.ts</automated>
  </verify>
  <acceptance_criteria>
    - `calculateMetrics` aceita 3 ou 4 argumentos e compila em ambos os casos.
    - `MetricsOptions` e `getSaleCampaign` são exportados.
    - `options.productId` igual a `undefined`, `null`, `''` ou `'all'` produz exatamente o mesmo resultado numérico da versão anterior (exceto o campo novo `global.productFilter === null`).
    - Com `productId` preenchido, `global.grossRevenue` soma apenas vendas aprovadas cujo `product_id` bate com o filtro.
    - Com `productId` preenchido, `global.adSpend` e `campaigns` consideram apenas campanhas presentes nos UTMs dessas vendas.
    - Com `productId` preenchido, `global.expenses === 0`.
    - Nenhum `import` novo em `src/lib/metrics.ts`.
  </acceptance_criteria>
  <done>Motor de métricas aceita recorte por produto sem alterar o comportamento sem filtro.</done>
</task>

<task type="auto">
  <name>Task 2: Harness offline do filtro por produto</name>
  <files>scripts/check-metrics-product-filter.mjs</files>
  <read_first>
    - scripts/check-hotmart-mapping.mjs (estrutura do harness: cabeçalho com comando tsc, criação de .tmp-verify/package.json, import dinâmico, blocos de assert)
    - src/lib/metrics.ts (versão já modificada na Task 1)
  </read_first>
  <action>
    Criar `scripts/check-metrics-product-filter.mjs` copiando a estrutura de `scripts/check-hotmart-mapping.mjs`:
    cabeçalho com o comando de compilação, garantia de `.tmp-verify/package.json` com `{"type":"module"}`, import dinâmico de `.tmp-verify/metrics.js` via `pathToFileURL`, mensagem de remediação em caso de falha de import, e `assert` de `node:assert/strict`.

    Comando de compilação documentado no cabeçalho:
    `npx tsc src/lib/metrics.ts --outDir .tmp-verify --module es2022 --target es2022 --moduleResolution bundler`

    Fixtures compartilhadas (definir uma vez no topo):
    - sales:
      1. `{ transaction_id: 'T1', status: 'APPROVED', price: 100, product_id: 'P1', sale_utms: { utm_campaign: 'camp-a' } }`
      2. `{ transaction_id: 'T2', status: 'APPROVED', price: 200, product_id: 'P1', sale_utms: [{ utm_campaign: 'camp-a' }] }`  // relação vinda como array
      3. `{ transaction_id: 'T3', status: 'APPROVED', price: 400, product_id: 'P2', sale_utms: { utm_campaign: 'camp-b' } }`
      4. `{ transaction_id: 'T4', status: 'REFUNDED', price: 999, product_id: 'P1', sale_utms: { utm_campaign: 'camp-a' } }`
      5. `{ transaction_id: 'T5', status: 'APPROVED', price: 50, product_id: null, sale_utms: null }`
    - adSpends: `{ campaign_id: 'camp-a', campaign_name: 'Camp A', spend: 100, impressions: 1000, clicks: 10 }` e `{ campaign_id: 'camp-b', campaign_name: 'Camp B', spend: 50, impressions: 500, clicks: 5 }`
    - expenses: `{ amount: 30 }`

    Como `AD_TAX_PERCENTAGE` default é 0.13, calcular os esperados a partir da constante exportada pelo módulo (`mod.AD_TAX_PERCENTAGE`) em vez de cravar números com imposto — assim o harness não quebra se a env mudar. Usar `assert.ok(Math.abs(a - b) < 1e-6, msg)` para comparações de ponto flutuante.

    Blocos de assertion obrigatórios:
    1. **Sem filtro** — `calculateMetrics(sales, adSpends, expenses)`: `global.salesCount === 4`, `global.grossRevenue === 750`, `global.expenses === 30`, `global.adSpend === 150 * (1 + tax)`, `global.productFilter === null`, `campaigns.length === 2`.
    2. **Compatibilidade de 3 argumentos vs `{ productId: null }`** — os dois `global.grossRevenue` são iguais; idem para `'all'` e para `''`.
    3. **Filtro P1** — `calculateMetrics(sales, adSpends, expenses, { productId: 'P1' })`: `global.salesCount === 2` (T4 é REFUNDED, T5 é outro produto), `global.grossRevenue === 300`, `global.expenses === 0`, `global.adSpend === 100 * (1 + tax)` (somente camp-a), `campaigns.length === 1`, `campaigns[0].campaign_id === 'camp-a'`, `campaigns[0].salesCount === 2` (prova que `sale_utms` como array também é atribuído), `global.productFilter === 'P1'`.
    4. **Filtro P2** — `global.grossRevenue === 400`, `campaigns.length === 1`, `campaigns[0].campaign_id === 'camp-b'`, `global.adSpend === 50 * (1 + tax)`.
    5. **Produto inexistente** — `{ productId: 'NAO-EXISTE' }`: `global.salesCount === 0`, `global.grossRevenue === 0`, `global.adSpend === 0`, `global.roas === 0`, `global.cpa === 0`, `campaigns.length === 0` e nenhum `NaN` em `global` (percorrer os valores numéricos com `Number.isFinite`).
    6. **Entradas vazias** — `calculateMetrics([], [], [], { productId: 'P1' })` não lança e devolve zeros finitos.

    Terminar o script com `console.log('All metrics product-filter checks passed');`.
  </action>
  <verify>
    <automated>npx tsc src/lib/metrics.ts --outDir .tmp-verify --module es2022 --target es2022 --moduleResolution bundler && node scripts/check-metrics-product-filter.mjs</automated>
    <automated>node scripts/check-metrics-product-filter.mjs && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-metrics-product-filter.mjs` existe e termina com exit code 0.
    - Os 6 blocos de assertion listados estão presentes e passam.
    - O script falha (exit != 0) se `calculateMetrics` for revertida para ignorar `options.productId` — validar removendo mentalmente o filtro: o bloco 3 quebraria em `grossRevenue`.
    - O harness não faz nenhuma chamada de rede nem acessa o Supabase.
  </acceptance_criteria>
  <done>Harness offline cobre filtro, compatibilidade retroativa e casos de borda; exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query string do dashboard → `options.productId` | Valor controlado pelo usuário chega até `calculateMetrics` (validação de formato acontece em 07-04) |
| Linhas do Postgres → `calculateMetrics` | Dados vindos do banco tratados como não confiáveis quanto ao formato (campos podem ser null) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | `calculateMetrics(options.productId)` | mitigate | Comparação estrita por igualdade de string (`String(s.product_id) === productFilter`); nunca é usado em construção de query, regex ou eval |
| T-07-06 | Denial of Service | `calculateMetrics` | mitigate | Complexidade linear no número de vendas; nenhum loop aninhado sobre `sales`; `Set` para lookup de campanha em O(1) |
| T-07-07 | Information Disclosure | retorno de `calculateMetrics` | mitigate | O retorno agrega valores; nenhum `transaction_id`, dado de comprador ou linha bruta é exposto no objeto de métricas |
| T-07-08 | Tampering | `getSaleCampaign` | mitigate | Trata `sale_utms` null/objeto/array defensivamente e devolve `null` em qualquer outro formato, evitando exceção não tratada que derrubaria a rota |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo é instalado (`package.json` inalterado); install descoberto durante a execução exige checkpoint humano bloqueante |
</threat_model>

<verification>
1. `npx tsc --noEmit` → exit 0
2. `npx tsc src/lib/metrics.ts --outDir .tmp-verify --module es2022 --target es2022 --moduleResolution bundler && node scripts/check-metrics-product-filter.mjs` → exit 0, imprime "All metrics product-filter checks passed"
3. `node scripts/check-hotmart-mapping.mjs` continua passando (nenhuma regressão cruzada)
</verification>

<success_criteria>
- `calculateMetrics` com `productId` devolve faturamento, lucro, ROAS, CPA e margem restritos ao produto.
- A lista `campaigns` reflete o filtro de produto.
- Chamadas sem o quarto argumento produzem os mesmos números de antes.
- Harness offline verde.
</success_criteria>

<output>
Create `.planning/phases/07-filtro-por-produto/07-02-SUMMARY.md` when done
</output>
